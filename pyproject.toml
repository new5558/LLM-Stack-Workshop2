[project]
name = "workshop2-rag"
version = "0.1.0"
description = "Minimal RAG and Search Workshop using OpenRouter and Qdrant"
authors = [
    {name = "Workshop Participant"}
]
dependencies = [
    "streamlit",
    "qdrant-client",
    "datasets",
    "tqdm",
    "requests",
    "openai",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
